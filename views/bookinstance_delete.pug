extends layout 

block content 

    h1 #{title}: ID:#{bookinstance._id}
    p Title: 
        a(href=bookinstance.book.url) #{bookinstance.book.title}
    strong Are you sure you want to delete this book instance?
    form.form-group(method="POST")
        div.form-group
            input#bookinstanceid.form-control(type="hidden" name="bookinstanceid" value=bookinstance._id)

        button.btn.btn-primary(type="submit") Delete